fix(step20): preserve DOCX text during Aspose banner cleanup

Strip evaluation notices with non-greedy matching so text between
repeated banners is retained. Add regression coverage for repeated
notices.

Validation: Restore-And-Preview passed; 148/148 tests passed.