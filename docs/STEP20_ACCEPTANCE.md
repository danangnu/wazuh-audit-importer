# Step 20 acceptance — candidate 1180019, missing-only baseline

## Scope and evidence

The September 24, 2026 08:56 UTC Step 16 read-only triage showed candidate `1180019` as pending, fingerprint matched, disk=2, eligible=2, Solr=1, match=1, missing=1, stale=0, other=0. The September 25 fresh review identified the missing file as `1180019 2025 08 19 Mclean, John Resume.docx`. The legacy `classDocument.ExtractTextUsingAspose` source has been ported into Step 10B with Aspose.Words 24.9.0 and the `ToString(SaveFormat.Text)` conversion. The Aspose Evaluation Only banner cleanup must use non-greedy matching: mutation 43's stored payload contained only 44 characters even though the verified UNC file hash matched the attached DOCX, whose package XML contains substantially more text. Never apply mutation 43. Deploy the corrected cleanup, pass `Restore-And-Preview.cmd` on MGMTNB08 and its regression test, then generate a fresh mutation and review its complete extracted text before apply. Step 20 remains limited to `1180019`. The remaining SmallDrift candidates `1180003`, `1180006`, `1180016`, and `1180017` have wider drift and remain gated. ModerateDrift, HighDrift, StaleCapture and already approved baselines remain outside this path.

Step 20 does not change the 25-candidate importer/FIM allowlist, existing Step 17/18/19 scopes, database schema, or the Step 11 manual Solr apply requirement. An older planned mutation 37 for candidate `1180021` was superseded by applied mutation 39; never apply mutation 37 or treat it as part of Step 20.

## Deploy and build on MGMTNB08

1. On MGMTNB08, run `Step13-Stop.cmd` as Administrator and verify the task and its supervisor, tunnel, and pipeline processes have stopped. Keep `ops/step13.local.json`, DPAPI secrets, existing state, reports and local configuration. Do not replace them with package defaults.
2. Deploy the Step 20 source to the existing project folder and run `Restore-And-Preview.cmd`. Require restore, .NET 9 build, all offline self-tests (148/148), and sample preview to pass before restarting Step 13. Aspose.Words 24.9.0 must restore successfully. The authoring workspace cannot connect to the user's MariaDB, Solr, or FLOSVR01.
3. Restart with `Start-ScheduledTask -TaskName 'WazuhAuditImporter-Step13'` and check `Step13-Status.cmd` for running supervisor, SSH tunnel and pipeline. Deployment and build do not approve any baseline or write to Solr.

## Review, approval, reconciliation

1. Run `Step20-Review-MissingOnly.cmd 1180019`. Inspect the current file path and expected Solr ID. Require `fingerprint_match=true`, `step20_approval=YES`, `eligible=2`, `match=1`, `missing=1`, `stale=0`, `other=0`, exactly one identifiable missing item, and a source extension supported by the current payload extractor (`.txt`, `.html`, `.htm`, `.doc`, `.docx`, `.docm`, or `.rtf`). Stop if evidence differs, the fingerprint changed, or the source uses `.pdf` or another unsupported extension. Use the **fresh** SHA-256 printed by an eligible review.
2. Preview `Step20-Approve-MissingOnly.cmd 1180019 <fresh-sha256>`. Verify it shows the same path and expected ID. Only after exact operator review, run `Step20-Approve-MissingOnly.cmd 1180019 <fresh-sha256> --ack-missing-only --apply`. Approval changes only the MariaDB baseline status, not Solr.
3. Observe Step 13. If its first worker snapshot yields `operation=none`, run read-only `recovery-inspect --candidate-id 1180019` to check live missing/stale evidence before using a **new**, unique, nonempty `DNI_` filtered trigger. Never alter a real candidate document to create an event. If event versions race or the latest mutation supersedes a planned mutation, do not apply the older plan. Stop Step 13, create one new filtered trigger while stopped, let events settle, then restart and review the latest mutation, as needed.
4. Once the latest mutation is `ReadyForApproval`, run `solr-execute --mutation-id <latest-id>` without `--apply`. Require `PREFLIGHT PASS`, no source/payload drift, the expected extractor (`legacy_aspose_words_24_9` for this `.docx`), and **only** the expected `index_document` for the missing current file; no delete and no `DNI_` action. Review the full extracted content for the candidate and confirm both legacy evaluation notices are absent. Stop if the extractor output is unexpectedly short or incomplete; do not apply a previously generated payload after an extractor fix.
5. The operator may then separately run `solr-execute --mutation-id <same-latest-id> --apply`. Require accepted POST/commit and post-commit GET verification. Run `recovery-inspect --candidate-id 1180019` and require the latest mutation `HealthyTerminal`, no failed actions, and live `missing=0, stale=0`.

Leave existing Step 17/18/19 and new Step 20 trigger files in place until cleanup is separately reviewed. Deleting them generates new FIM events. A processing, failed, superseded, or uncertain mutation is never authorized for a blind retry.
